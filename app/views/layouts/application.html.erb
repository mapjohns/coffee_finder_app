<!DOCTYPE html>
<html>
  <head>
    <title>CoffeeFinderApp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all' %>
  </head>
  <div><h3 id="topbar"><%= link_to "Home", '/'%> | <%= link_to "Coffee", "/coffees" %> | <%= top_bar %> | <%= my_profile %></h3></div>
  <body>
    <%= yield %>
  </body>
</html>
